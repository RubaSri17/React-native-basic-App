import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  imageStyle: {
    width: 70,
    height: 80,
    marginTop: 60,
    marginBottom: 30,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});
